<UserControl x:Class="TheMuseum.Biography.Components.Artwork"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:m="clr-namespace:TheMuseum.Biography.Models"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance m:ArtworkModel}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
       
        </Grid.RowDefinitions>
        <TextBlock Text="{Binding Name}" />
        <TextBlock Text="{Binding Description}" TextWrapping="Wrap" Grid.Row="1"/>
    </Grid>
</UserControl>
